// no need to write as Chai.jsx (but for our convenience we write as .jsx for new inserting files)
// in CRA format : we need to just name the inserted fle with capitail and the function name inside of it is also need to start with capital letter
function Chai(){
    return(
        <h2>chai in react </h2>
    )
}

export default Chai